apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: n8n
  version: 1.0.1

type: n8n-workflow

sourceRoot: "workflows"

provides:
  workflows:
    - definitionFile: tank-reconciliation-agent.n8n.json
      name: tank-reconciliation-agent
